import LoadingIcon from '../assets/loading.gif'

const loading = () => {
    return(
        <div id="__loading">
            <img src={LoadingIcon} alt="loading" />
        </div>
    )
}

export default loading;